id="dw-txs"
ssh_key_name=id_rsa
master=k8s-dw-txs-master-01
os_user=centos
name=${dir##*/}
namespace=${name}
yaml=${dir}/${name}.yaml
inyaml=${dir}/${name}-template.yaml
buildyaml=${dir}/${name}-build.yaml
domain=cluster.local
hubuid=davidwalter0
k8srepo=git@${id}k8s-git-repo:${name}.git
hubrepo=git@github.com:${hubuid}/${name}.git
